name: PR Landing

on:
  pull_request:
    paths:
      - "web/landing-pages/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/landing-pages/danova-lead/package-lock.json

      - name: Install dependencies
        working-directory: web/landing-pages/danova-lead
        run: npm ci

      - name: Lint
        working-directory: web/landing-pages/danova-lead
        run: npm run lint

      - name: Typecheck
        working-directory: web/landing-pages/danova-lead
        run: npx tsc --noEmit

      - name: Build
        working-directory: web/landing-pages/danova-lead
        run: npm run build
